<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1A1A1A"
    android:padding="16dp"
    android:fitsSystemWindows="true">

    <!-- Title bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="24dp">

        <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@mipmap/ic_launcher"
            android:layout_marginEnd="12dp" />

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:maxLines="2"
            android:ellipsize="end"
            android:text="@string/remote_now_playing" />
    </LinearLayout>

    <!-- Video mode buttons -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/remote_3d_mode"
        android:textColor="#888888"
        android:textSize="12sp"
        android:layout_marginBottom="8dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="32dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSbs"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="56dp"
            android:layout_marginEnd="8dp"
            android:text="@string/remote_ou_to_sbs"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:checkable="true" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnResizeMode"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="56dp"
            android:layout_marginEnd="8dp"
            android:text="@string/remote_auto"
            android:textAllCaps="false"
            android:textColor="@android:color/white" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnShift"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="56dp"
            android:text="@string/remote_shift"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:checkable="true" />
    </LinearLayout>

    <!-- Playback controls -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="24dp">

        <ImageButton
            android:id="@+id/btnRewind"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_rew"
            android:tint="@android:color/white"
            android:scaleType="centerInside"
            android:contentDescription="@string/player_rewind" />

        <ImageButton
            android:id="@+id/btnPlayPause"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_marginStart="32dp"
            android:layout_marginEnd="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_pause"
            android:tint="@android:color/white"
            android:scaleType="centerInside"
            android:contentDescription="@string/player_play_pause" />

        <ImageButton
            android:id="@+id/btnForward"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_ff"
            android:tint="@android:color/white"
            android:scaleType="centerInside"
            android:contentDescription="@string/player_forward" />
    </LinearLayout>

    <!-- Seek bar -->
    <SeekBar
        android:id="@+id/seekBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:progressTint="#2196F3"
        android:thumbTint="#2196F3" />

    <!-- Time display -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="32dp">

        <TextView
            android:id="@+id/tvPosition"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#AAAAAA"
            android:textSize="14sp"
            android:text="0:00" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/tvDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#AAAAAA"
            android:textSize="14sp"
            android:text="0:00" />
    </LinearLayout>

    <!-- Lazy 3D (head-tracking parallax). Toggled only on the phone-side remote because
         it depends on the glasses being attached and the phone being free to start IMU. -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnLazy3d"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginBottom="12dp"
        android:text="@string/remote_lazy3d"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:checkable="true"
        android:visibility="gone" />

    <!-- Stream quality picker — shown only for sources that expose ≥2 qualities (VK/OK.ru).
         Visibility is driven from RemoteControlActivity.updateButtons(). -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnQuality"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginBottom="12dp"
        android:text="@string/quality"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        app:icon="@drawable/ic_quality_24"
        app:iconTint="@android:color/white"
        android:visibility="gone" />

    <!-- Audio track button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnAudio"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="@string/remote_audio_track"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        app:icon="@drawable/ear_sound_24px"
        app:iconTint="@android:color/white" />

    <!-- Subtitles button — subtitles default to off, so this is how the user turns them on
         while the picture is on the goggles and the phone is the remote. -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSubtitle"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="12dp"
        android:text="@string/menu_subtitle"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        app:icon="@drawable/ic_subtitle_24"
        app:iconTint="@android:color/white" />

    <!-- Volume boost — lifts quiet sources (low-mastered web video, goggle speakers). Persists. -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnVolumeBoost"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="12dp"
        android:text="@string/volume_boost_off"
        android:textAllCaps="false"
        android:textColor="@android:color/white" />

    <!-- Lazy 3D sensor debug readout (TEMPORARY tuning aid — remove once head-tracking is dialled in).
         Hidden unless Lazy 3D is on; shows live IMU rates and the resulting parallax shift. -->
    <TextView
        android:id="@+id/tvLazyDebug"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:fontFamily="monospace"
        android:textColor="#66FF99"
        android:textSize="12sp"
        android:lineSpacingExtra="2dp"
        android:visibility="gone" />

    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Stop button — kept to 1/3 width and centred so it's hard to hit by accident (e.g. when a
         system USB-connection prompt pops up over the bottom of the screen). -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Space
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnStop"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:text="@string/remote_stop_playback"
            android:textAllCaps="false"
            android:textColor="#FF5555"
            app:strokeColor="#FF5555" />

        <Space
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />
    </LinearLayout>

</LinearLayout>
